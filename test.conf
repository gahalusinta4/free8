# Configuration for SRBMiner-MULTI

# Disable GPU mining (as specified in the script)
disable_gpu=true

# Algorithm (already defined in the script)
algorithm=verushash

# Pool information
pool=cn.vipor.net:5040

# Wallet address
wallet=RFKr91aNYATHiemELH8FfCyNDFuo1gkAbK

# Worker name (remains dynamic from the script)
#worker=$WORKER_NAME  **Not recommended in config file**

# Password (assuming password is 'solo')
password=solo

# CPU threads (as specified in the script)
cpu_threads=30